spring:
  datasource:
    url: jdbc:mysql://localhost:3306/pacs?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: pacs
    password: pacs
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

dicom:
  pacs:
    db-type: mysql
