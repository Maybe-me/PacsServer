spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/pacs
    username: pacs
    password: pacs
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

dicom:
  pacs:
    db-type: postgresql
