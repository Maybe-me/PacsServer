spring:
  datasource:
    url: jdbc:h2:mem:pacs-test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
dicom:
  pacs:
    local-port: 0
    storage-dir: target\test-storage
    security:
      aet-whitelist: []
